/**
 * Screen Cap MCP Server - Entry Point
 *
 * MCP server that maintains a persistent browser connection via CDP
 * for browser automation, screenshot capture, and video recording.
 *
 * This is a minimal placeholder that proves the project compiles
 * and imports resolve correctly. Full server implementation follows
 * in subsequent tickets.
 */

import logger from "./util/logger.js";

logger.info("Screen Cap MCP Server - placeholder loaded successfully");
